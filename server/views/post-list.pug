extends layout.pug

block content
  ul.post-list
    each post in posts
      li.post
        h2
          a(href=post.url)= post.title
        p Submitted by #{post.user.username}
        p Score: #{post.voteScore}